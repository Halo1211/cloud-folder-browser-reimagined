# Local release output

From the repository root, run `./tools/Build-Release.ps1` to create the Windows x64 release files in this directory.

Generated executables, ZIP files, configuration files, and checksums are intentionally ignored by Git. Upload them through the repository's GitHub Releases page instead of committing them to the source tree.
